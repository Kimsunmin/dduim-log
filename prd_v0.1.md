좋아! 서비스 기획과 디자인 방향성, 기술 스택까지 완전히 싱크를 맞췄으니, AI 코드 생성 툴이나 컴포넌트 빌더(v0, Claude 등)에 통째로 컨텍스트를 주입할 수 있는 최종 제품 요구사항 명세서(PRD)를 작성해 줄게.

이 내용을 기반으로 프로젝트의 뼈대와 핵심 비즈니스 로직을 빌드업하면 돼.

---

# 📝 제품 요구사항 명세서 (PRD): 뜀로그 (dduim.log)

## 1. 프로젝트 개요

* **서비스명:** 뜀로그 (dduim.log)
* **한 줄 정의:** 러너들이 나만의 동네 꿀코스를 직접 그리고 공유하는 모바일 최적화 웹 플랫폼
* **기획 의도:** 나이키 런 클럽(NRC) 등 기존 앱의 기록 중심 피드에서 벗어나, 광고 없는 '진짜 러닝 코스 경로 정보'를 지도와 인스타 감성의 피드로 아카이빙하고 탐색하는 갈증을 해결한다.
* **개발 형태:** 1인 개발 최적화 MVP (최소 기능 제품)

## 2. 타겟 유저 및 핵심 페인 포인트

* **핵심 타겟:** 특정 지역(ex: 망원한강공원 등)의 검증된 꿀코스를 찾고 싶거나, 내가 달린 코스를 자랑하고 싶은 러너
* **페인 포인트:** 블로그나 인스타에서 코스를 검색하면 광고성 글이 너무 많고, 정작 정확한 GPX 경로 정보나 러닝 특화 인프라 정보(신호등 여부, 가로등 유무 등)를 한눈에 파악하기 어려움.

## 3. 핵심 기술 스택 및 아키텍처 (비용 $0 방어형)

* **Frontend / Backend:** Next.js (App Router, Full-stack 활용으로 인프라 분리 공수 차단)
* **Hosting:** Vercel (Git push 기반 배포, 프리티어 기준 고정 서버비 $0)
* **Database:** Supabase (PostgreSQL + PostGIS 공간 확장 기능 활성화, 초기 DB 비용 $0)
* **Map API:** 카카오 지도 Web API (한국 지형/지명 최적화, 일일 무료 할당량 30만 건 활용)
* **인증 및 상태 관리:** 로그인 연동 없음 (초기 이탈 차단) / 브라우저 `LocalStorage` 기반 로컬 즐겨찾기 상태 관리

## 4. 핵심 기능 요구사항 (Scope)

### ① 메인 화면 (탐색 모드)

* **Desktop Mobile View 인터페이스:** 데스크톱/태블릿 접속 시에도 화면 중앙에 가로 고정 `max-w-[430px]`의 아이폰 뷰를 렌더링하고 양옆은 깔끔한 배경 및 브랜딩 영역으로 고정.
* **지도-피드 연동 레이아웃 (Map-as-a-Background):** 배경 전체에 카카오 지도가 깔리고, 그 위에 스와이프 가능한 인스타 감성의 코스 카드 피드(바텀 시트)가 얹어지는 2개 레이어 구조.
* **시작점 마커 렌더링:** 지도가 로드되면 현재 맵 영역(Bounds) 내에 존재하는 코스들의 시작 지점만 귀여운 커스텀 마커(러닝화 모양 배지)로 지도 위에 핀을 꽂음. 전체 경로(선)를 한 번에 다 그리지 않아 모바일 성능 및 렌더링 최적화.
* **인터랙션 싱크:** 하단 피드 카드를 넘기거나 특정 시작점 핀을 터치하면, 카카오 지도가 해당 코스로 부드럽게 이동(`map.panTo`)하며 해당 코스의 전체 경로(`kakao.maps.Polyline`)를 지도 위에 네온 컬러로 하이라이트 노출.

### ② 코스 미리 그리기 (생산자 모드)

* **자석식 스냅 그리기 (Phase 1 직선 그리기 우선):** 화면 중앙의 십자선이나 지도 클릭을 통해 포인트를 찍으면 순차적으로 선이 연결되는 인터페이스 구현. (추후 카카오 모빌리티 도보 길찾기 API 등으로 도로 스냅 고도화 구조 대비)
* **경로 메타데이터 자동 계산:** 사용자가 점을 찍어 경로가 갱신될 때마다 전체 좌표 배열을 기반으로 총 거리(km)와 **예상 소요 시간**(러너 평균 5분 30초 페이스 기준 계산값)을 백엔드단에서 계산해 실시간 화면 표시.
* **발행 및 데이터 저장:** 코스 이름, 설명, 러닝 특화 메타 태그(`#신호등제로 🚦`, `#가로등빵빵 💡`, `#한강뷰 🌊`)를 입력 후 '뜀로그 발행' 버튼을 누르면 Supabase의 PostGIS 공간 데이터 타입(`GEOMETRY(LineString, 4326)`)으로 저장.

### ③ 비로그인 즐겨찾기 (북마크)

* **LocalStorage 기반 저장:** 피드 카드의 귀여운 스마일 즐겨찾기 아이콘을 누르면, 브라우저 LocalStorage 내 `my-favorite-routes` 배열에 코스 ID가 토글(`push/filter`)되어 유저 고유 데이터 유지.
* **보관함 탭:** 사용자가 저장한 ID 배열 목록을 기반으로 Supabase에서 해당 코스들만 필터링하여 인스타 피드로 모아보는 '내 저장 목록' 기능 제공.

## 5. UI/UX 디자인 및 보이스 톤 가이드

* **비주얼 톤:** 따뜻한 크림색/웜그레이 베이스의 배경과 파스텔톤(민트 블루, 소프트 옐로우 등) 포인트를 믹스한 **말랑말랑하고 귀여운 무드**. 모서리는 `rounded-3xl` 등으로 매우 둥글게 처리하고 부드러운 그림자(네오모피즘 스타일) 적용.
* **보이스 톤:** 딱딱한 시스템 어투 대신 친근하고 담백한 어조 활용. (ex: "등록하기" ➡️ "뜀로그 발행", "코스를 찾으세요" ➡️ "어디로 뛸까?")

## 6. 데이터 모델 (Supabase / PostGIS 예시 사양)

* **`RunningRoute` 테이블:**
* `id`: UUID 또는 BIGINT (Primary Key)
* `title`: VARCHAR (코스 제목)
* `distance`: FLOAT (총 거리 km)
* `expected_time`: INT (예상 소요 시간, 분 단위)
* `tags`: TEXT[] (메타데이터 태그 배열)
* `start_point`: `GEOMETRY(Point, 4326)` (조회용 시작 위치 좌표, 인덱스 적용)
* `path`: `GEOMETRY(LineString, 4326)` (실제 코스 상세 경로 전체 좌표 데이터)



---

이 명세서를 이제 코덱스나 생성형 AI 툴에 프롬프트로 입력하면 설계 의도를 100% 이해하고 코드를 짜줄 거야. 자, 그럼 레포지토리 파고 첫 커밋 찍으러 가자! 개발하다가 데이터베이스 공간 쿼리나 카카오 지도 연동에서 막히는 부분 있으면 언제든 코드 들고 찾아와! 🚀