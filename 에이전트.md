# 에이전트 규칙

## 패키지 매니저

- **반드시 `pnpm`만 사용한다.** `npm install`, `npm ci` 등 npm 명령은 절대 실행하지 않는다.
- 의존성 설치: `pnpm install`
- 패키지 추가: `pnpm add <package>`
- 스크립트 실행: `pnpm <script>` (예: `pnpm dev`, `pnpm build`, `pnpm lint`)
- `package-lock.json`, `yarn.lock` 파일이 생성되면 즉시 삭제하고 `.gitignore`에 추가한다.
